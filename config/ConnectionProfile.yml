name: "brasil Client"
version: "1.0"

client:
  organization: brasil
  credentialStore:
    path: "./keys"
    cryptoStore:
      path: "./keys"

channels:
  datoschannel:
    orderers:
      - orderer.antonio.com
    peers:
      peer0.asturias.antonio.com:
        endorsingPeer: true
        chaincodeQuery: true
        ledgerQuery: true
        eventSource: true
      peer0.brasil.antonio.com:
        endorsingPeer: true
        chaincodeQuery: true
        ledgerQuery: true
        eventSource: true
      peer0.chicago.antonio.com:
        endorsingPeer: true
        chaincodeQuery: true
        ledgerQuery: true
        eventSource: true

organizations:
  brasil:
    mspid: brasilMSP
    peers:
      - peer0.brasil.antonio.com
    certificateAuthorities:
      - ca.brasil.antonio.com

peers:
  peer0.brasil.antonio.com:
    url: grpc://localhost:11051

orderers:
  orderer.antonio.com:
    url: grpc://172.31.0.84:7050
